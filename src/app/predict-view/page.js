import React from 'react';
import PredictVideoViewContainer from './PredictVideoViewContainer';

const PredictViewPage = () => {
  return <PredictVideoViewContainer />;
};

export default PredictViewPage;
